{
	"scripts": {
		"lint": "eslint . --ext .ts,.tsx",
		"compile": "deno compile --allow-env --allow-read --allow-write"
	},
	"devDependencies": {
		"@wvbe/eslint-config": "github:wvbe/eslint-config",
		"eslint": "^8.16.0"
	}
}
