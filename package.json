{
	"scripts": {
		"test": "npm run deno:test --silent",
		"lint": "eslint . --ext .ts,.tsx",
		"deno:test": "deno test --allow-read libraries",
		"deno:lint": "deno lint libraries",
		"deno:compile": "deno compile --allow-env --allow-read --allow-write"
	},
	"devDependencies": {
		"@wvbe/eslint-config": "github:wvbe/eslint-config",
		"eslint": "^8.16.0"
	}
}
